---
phase: 01-shared-build-infrastructure
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - infrastructure/gradle-platform/src/functionalTest/kotlin/eu/servista/gradle/LibraryPluginTest.kt
  - infrastructure/gradle-platform/src/functionalTest/kotlin/eu/servista/gradle/ApiServiceCompositionTest.kt
  - infrastructure/gradle-platform/src/functionalTest/kotlin/eu/servista/gradle/EventSinkCompositionTest.kt
  - infrastructure/gradle-platform/src/functionalTest/kotlin/eu/servista/gradle/PipelineCompositionTest.kt
  - architecture/decisions/017-backend-tech-stack.md
autonomous: true
requirements: [FOUND-02]

must_haves:
  truths:
    - "TestKit test verifies servista.library plugin applies Kotlin/JVM 21 and directory structure validation"
    - "TestKit test verifies API service composition (api-service + jooq + kafka-producer) compiles a minimal Kotlin source"
    - "TestKit test verifies event sink composition (library + kafka-consumer + avro + testing + observability) compiles"
    - "TestKit test verifies pipeline composition (pipeline-service + avro + testing + observability) compiles"
    - "TestKit test verifies build fails when required directory structure is missing"
    - "ADR-017 is amended to replace monorepo with multi-repo + gradle-platform approach"
    - "./gradlew check passes (compiles all plugins AND runs all TestKit tests)"
  artifacts:
    - path: "infrastructure/gradle-platform/src/functionalTest/kotlin/eu/servista/gradle/LibraryPluginTest.kt"
      provides: "Base plugin TestKit tests: Kotlin/JVM config, directory validation failure"
      contains: "GradleRunner"
    - path: "infrastructure/gradle-platform/src/functionalTest/kotlin/eu/servista/gradle/ApiServiceCompositionTest.kt"
      provides: "API service composition TestKit test matching Phase 3 success criteria"
      contains: "servista.api-service"
    - path: "infrastructure/gradle-platform/src/functionalTest/kotlin/eu/servista/gradle/EventSinkCompositionTest.kt"
      provides: "Event sink composition TestKit test"
      contains: "servista.kafka-consumer"
    - path: "infrastructure/gradle-platform/src/functionalTest/kotlin/eu/servista/gradle/PipelineCompositionTest.kt"
      provides: "Pipeline composition TestKit test"
      contains: "servista.pipeline-service"
    - path: "architecture/decisions/017-backend-tech-stack.md"
      provides: "Amended ADR reflecting multi-repo + gradle-platform decision"
      contains: "multi-repo"
  key_links:
    - from: "ApiServiceCompositionTest.kt"
      to: "Convention plugin classpath"
      via: "GradleRunner.create().withPluginClasspath()"
      pattern: "withPluginClasspath"
    - from: "ADR-017"
      to: "gradle-platform repo"
      via: "Monorepo Structure section replaced with multi-repo description"
      pattern: "gradle-platform"
---

<objective>
Create Gradle TestKit functional tests verifying all major plugin compositions, and amend ADR-017 to reflect the multi-repo decision. This plan proves the convention plugins work end-to-end and documents the architectural change.

Purpose: TestKit tests serve as the Phase 3 success criteria verification -- "a hello world service builds successfully" is proven by TestKit tests that create minimal projects, apply plugin compositions, and verify compilation. ADR-017 amendment ensures the architecture documentation matches the actual multi-repo approach.
Output: 4 TestKit test classes covering all composition patterns, amended ADR-017, and a passing `./gradlew check`.
</objective>

<execution_context>
@/home/sven/.claude-pp/get-shit-done/workflows/execute-plan.md
@/home/sven/.claude-pp/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-shared-build-infrastructure/03-CONTEXT.md
@.planning/phases/03-shared-build-infrastructure/03-02-SUMMARY.md
@architecture/decisions/017-backend-tech-stack.md
@infrastructure/gradle-platform/build.gradle.kts

<interfaces>
<!-- From Plans 01-02: The complete convention plugin set -->

Convention plugins available (all registered in gradlePlugin block):
- servista.library: Kotlin 2.3/JVM 21, detekt, ktfmt, directory validation, coroutines, datetime
- servista.api-service: Ktor, Koin, kotlinx.serialization (applies servista.library)
- servista.jooq: jOOQ, HikariCP, Flyway, PostgreSQL JDBC (applies servista.library)
- servista.kafka-producer: Kafka clients (applies servista.library)
- servista.kafka-consumer: Kafka clients (applies servista.library)
- servista.pipeline-service: Kafka Streams (applies servista.library)
- servista.avro: Avro, Apicurio SerDes (applies servista.library)
- servista.testing: JUnit 5, Testcontainers, MockK, Kotest, Ktor test host (applies servista.library)
- servista.observability: kotlin-logging, Logback, Micrometer (applies servista.library)

Functional test source set is already configured in build.gradle.kts (from Plan 01):
- Source: src/functionalTest/kotlin/
- Dependencies: extends testImplementation (gradleTestKit, JUnit 5, Kotest assertions)
- Task: functionalTest (wired to check)
- TestKit classpath: injected via gradlePlugin.testSourceSets
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Gradle TestKit functional tests for plugin compositions</name>
  <files>
    infrastructure/gradle-platform/src/functionalTest/kotlin/eu/servista/gradle/LibraryPluginTest.kt,
    infrastructure/gradle-platform/src/functionalTest/kotlin/eu/servista/gradle/ApiServiceCompositionTest.kt,
    infrastructure/gradle-platform/src/functionalTest/kotlin/eu/servista/gradle/EventSinkCompositionTest.kt,
    infrastructure/gradle-platform/src/functionalTest/kotlin/eu/servista/gradle/PipelineCompositionTest.kt
  </files>
  <behavior>
    LibraryPluginTest:
    - Test 1: Applying servista.library with required directory structure succeeds (compileKotlin SUCCESS)
    - Test 2: Applying servista.library WITHOUT required directory structure fails with "Missing required directories" error message

    ApiServiceCompositionTest (MATCHES PHASE 3 SUCCESS CRITERIA #3):
    - Test 1: Applying servista.api-service + servista.jooq + servista.kafka-producer compiles a minimal Kotlin file successfully
    - Test 2: (optional) Verify that Ktor and jOOQ are on the classpath by checking dependency resolution

    EventSinkCompositionTest:
    - Test 1: Applying servista.kafka-consumer + servista.avro + servista.testing + servista.observability compiles successfully (typical event sink like svc-audit-writer)

    PipelineCompositionTest:
    - Test 1: Applying servista.pipeline-service + servista.avro + servista.testing + servista.observability compiles successfully (typical pipeline like silver-streaming)
  </behavior>
  <action>
    Create the `src/functionalTest/kotlin/eu/servista/gradle/` directory structure.

    Each test class follows the same pattern:
    1. Use `@TempDir` for project directory
    2. In `@BeforeEach`, create:
       - `settings.gradle.kts` with `rootProject.name = "test-xxx"`
       - Required directory structure: `src/main/kotlin/`, `src/main/resources/`, `src/test/kotlin/`
       - `build.gradle.kts` applying the relevant plugin composition
       - A minimal Kotlin source file in `src/main/kotlin/App.kt`:
         ```kotlin
         package eu.servista.test
         fun main() { println("Hello") }
         ```
    3. Use `GradleRunner.create()` with `.withProjectDir(projectDir)`, `.withPluginClasspath()`, `.withArguments("compileKotlin", "--stacktrace")`, `.forwardOutput()`
    4. Assert `result.task(":compileKotlin")?.outcome shouldBe TaskOutcome.SUCCESS` (using Kotest assertions)

    **LibraryPluginTest** has an additional negative test:
    - Delete `src/main/resources/` directory
    - Use `.buildAndFail()` instead of `.build()`
    - Assert `result.output` contains "Missing required directories" or "Required directory" error text

    **ApiServiceCompositionTest** is the critical test matching Phase 3 success criteria #3:
    ```
    plugins {
        id("servista.api-service")
        id("servista.jooq")
        id("servista.kafka-producer")
    }
    ```

    **EventSinkCompositionTest:**
    ```
    plugins {
        id("servista.kafka-consumer")
        id("servista.avro")
        id("servista.testing")
        id("servista.observability")
    }
    ```
    Note: Does NOT apply `servista.library` explicitly -- it is inherited from the domain plugins.

    **PipelineCompositionTest:**
    ```
    plugins {
        id("servista.pipeline-service")
        id("servista.avro")
        id("servista.testing")
        id("servista.observability")
    }
    ```

    Use JUnit 5 `@Test` annotations and Kotest `shouldBe` for assertions. Import:
    - `org.gradle.testkit.runner.GradleRunner`
    - `org.gradle.testkit.runner.TaskOutcome`
    - `org.junit.jupiter.api.BeforeEach`
    - `org.junit.jupiter.api.Test`
    - `org.junit.jupiter.api.io.TempDir`
    - `io.kotest.matchers.shouldBe`
    - `io.kotest.matchers.string.shouldContain`

    After creating all test files, run `./gradlew functionalTest` to verify all tests pass. If any test fails due to plugin compilation issues (not test logic), investigate the convention plugin scripts first. Common issues:
    - Double application of servista.library (when multiple domain plugins each apply it) should work fine -- Gradle handles idempotent plugin application
    - Missing directories in temp project (ensure @BeforeEach creates them)
    - Plugin classpath not injected (ensure functionalTest source set is registered via testSourceSets)
  </action>
  <verify>
    <automated>cd infrastructure/gradle-platform && ./gradlew functionalTest --no-daemon 2>&1 | tail -20</automated>
  </verify>
  <done>
    - 4 TestKit test classes exist in src/functionalTest/
    - All tests pass via ./gradlew functionalTest
    - API service composition test proves Phase 3 success criteria #3 (api-service + jooq + kafka-producer compiles)
    - Directory validation failure test proves enforcement works
    - Event sink and pipeline composition tests prove composability
  </done>
</task>

<task type="auto">
  <name>Task 2: Amend ADR-017 to reflect multi-repo with gradle-platform</name>
  <files>
    architecture/decisions/017-backend-tech-stack.md
  </files>
  <action>
    Update ADR-017 to replace the monorepo decision with the multi-repo + gradle-platform approach. This is required by Phase 3 success criteria #4 and the CONTEXT.md locked decision.

    **Changes required:**

    1. **Status line:** Keep "Accepted" but add amendment date:
       `**Status:** Accepted (amended 2026-03-03: monorepo replaced with multi-repo + gradle-platform)`

    2. **Decision section title (Section 2: Build System):**
       Replace "Convention plugins live in `build-logic/` as a composite build" with:
       "Convention plugins live in the standalone `gradle-platform` repository, published to Forgejo Maven registry. Each service repo consumes plugins and a version catalog via `pluginManagement` and `dependencyResolutionManagement` in their `settings.gradle.kts`."

    3. **Convention plugin list:** Update from 3 plugins to 9:
       Keep `servista.api-service`, `servista.pipeline-service`, `servista.library`, and add:
       `servista.jooq`, `servista.kafka-producer`, `servista.kafka-consumer`, `servista.avro`, `servista.testing`, `servista.observability`
       Add brief descriptions for each.

    4. **Monorepo Structure section:** Replace entirely with "Multi-repo Structure":
       ```
       ### Multi-repo Structure

       Services live in separate Forgejo repositories (created in Phase 1). Shared build
       infrastructure is in the `gradle-platform` repository:

       gradle-platform/                 # Standalone repo: convention plugins + version catalog
         build.gradle.kts               # kotlin-dsl + java-gradle-plugin + maven-publish + version-catalog
         catalog/libs.versions.toml     # Published version catalog (all dependency versions)
         src/main/kotlin/               # 9 precompiled script convention plugins
         src/functionalTest/kotlin/     # Gradle TestKit integration tests

       Each service repo:
         settings.gradle.kts            # pluginManagement + versionCatalogs from Forgejo
         build.gradle.kts               # Applies servista.* plugins, uses libs.* catalog
       ```
       Remove the old monorepo structure tree and the note about adapters living in the monorepo.

    5. **Alternatives Considered - Multi-repo entry:** Replace the negative framing with the actual decision:
       Change: `**Multi-repo over monorepo:** Shared library versioning nightmare with 10+ services...`
       To: `**Monorepo:** Rejected. Forgejo already provides a Maven package registry. Small, focused repos are preferred. Shared configuration is published from `gradle-platform` to Forgejo Maven registry and consumed by each service repo. This avoids monorepo tooling complexity while preserving version consistency through the published version catalog.`

    6. **Consequences section:** Update relevant bullet points:
       - Change "Phase 3 reads this ADR's version compatibility matrix to populate `libs.versions.toml`" to reflect that gradle-platform contains the catalog
       - Update "Convention plugins (...) encapsulate common dependencies" to list all 9 plugins
       - Add: "Each service repo configures Forgejo Maven registry in settings.gradle.kts to resolve convention plugins and version catalog"

    **Do NOT change:** Version compatibility matrix tables, technology selections, alternatives for language/framework choices, or any version numbers. The amendment is strictly about repo strategy (monorepo -> multi-repo) and plugin inventory (3 -> 9).
  </action>
  <verify>
    <automated>grep -c "multi-repo\|gradle-platform\|Multi-repo" architecture/decisions/017-backend-tech-stack.md</automated>
  </verify>
  <done>
    - ADR-017 status shows amendment date
    - Monorepo Structure section replaced with Multi-repo Structure
    - Convention plugin list expanded from 3 to 9
    - Build System section references gradle-platform repo and Forgejo Maven registry
    - Alternatives Considered updated to show monorepo as rejected
    - Version compatibility matrix unchanged
  </done>
</task>

</tasks>

<verification>
1. `cd infrastructure/gradle-platform && ./gradlew check` passes (compiles all plugins AND runs all TestKit tests)
2. `./gradlew functionalTest` runs at least 5 test cases (2 library, 1 api-service composition, 1 event sink, 1 pipeline)
3. `grep "multi-repo" architecture/decisions/017-backend-tech-stack.md` finds matches
4. `grep "gradle-platform" architecture/decisions/017-backend-tech-stack.md` finds matches
5. ADR-017 no longer contains "servista-backend/" monorepo structure tree
</verification>

<success_criteria>
- All Gradle TestKit functional tests pass
- API service composition (api-service + jooq + kafka-producer) verified by TestKit
- Directory structure validation enforcement verified by negative test
- ADR-017 amended to document multi-repo + gradle-platform approach
- ./gradlew check passes end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/03-shared-build-infrastructure/03-03-SUMMARY.md`
</output>

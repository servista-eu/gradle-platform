group=eu.servista
version=0.1.0

# Forgejo Maven registry authentication
# Set the token in one of:
#   1. ~/.gradle/gradle.properties: forgejo.token=<your-token>
#   2. Environment variable: FORGEJO_TOKEN=<your-token>
# Do NOT put the actual token in this file (committed to version control).
